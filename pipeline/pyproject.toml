[project]
name = "pipeline"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Arnav Chaudhary", email = "113378266+SaToRu-GoJo01@users.noreply.github.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pandas>=2.3.3",
    "psycopg2-binary>=2.9.13",
    "pyarrow>=21.0.0",
    "sqlalchemy>=2.0.53",
    "tqdm>=4.70.1",
]

[project.scripts]
pipeline = "pipeline:main"

[build-system]
requires = ["uv_build>=0.12.11,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
    "pgcli>=4.6.0",
]
